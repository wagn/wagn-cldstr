#!/bin/sh

APP_DIR=$CLDHOME/apps/wagn-cldstr/
DISTRO=$1

if [ -z $DISTRO ]; then
  echo "usage: build_mods <distro>"
  exit 1
fi


cd $APP_DIR

for MOD_DIR in wagn-*
do
  cd $MOD_DIR
  cloud_build $DISTRO
  cd -
done
 

